{% extends 'base.html.twig' %}

{% block title %}Reservationrepas index{% endblock %}

{% block body %}
    <h1>Reservationrepas index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Date</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for reservationrepa in reservationrepas %}
            <tr>
                <td>{{ reservationrepa.id }}</td>
                <td>{{ reservationrepa.date ? reservationrepa.date|date('Y-m-d') : '' }}</td>
                <td>
                    <a href="{{ path('reservationrepas_show', {'id': reservationrepa.id}) }}">show</a>
                    <a href="{{ path('reservationrepas_edit', {'id': reservationrepa.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('reservationrepas_new') }}">Create new</a>
{% endblock %}
