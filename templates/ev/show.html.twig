{% extends 'base.html.twig' %}

{% block title %}Ev{% endblock %}

{% block body %}
    <h1>Ev</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ev.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ ev.Nom }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ ev.date ? ev.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Lieu</th>
                <td>{{ ev.lieu }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ ev.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ev_index') }}">back to list</a>

    <a href="{{ path('ev_edit', {'id': ev.id}) }}">edit</a>

    {{ include('ev/_delete_form.html.twig') }}
{% endblock %}
