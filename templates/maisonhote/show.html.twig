{% extends 'baseback.html.twig' %}

{% block title %}Info Maison d'hote{% endblock %}

{% block body %}
    <h1>Information Maison d'hote</h1>


<div class="app-content">
                    <div class="side-app">
                        <div class="page-header">
                            <h4 class="page-title"></h4>
                            <ol class="breadcrumb">
                                <li class="breadcrumb-item"><a href="#"><i class="fe   fe-box mr-1"></i> Tables</a></li>
                                <li class="breadcrumb-item active" aria-current="page">Data Tables</li>
                            </ol>
                        </div>
                        <div class="row">
                            <div class="col-md-12 col-lg-12">
                                <div class="card">
                                    <div class="card-header">
                                        <div class="card-title">Information de la résevation :</div>
                                    </div>
                                    <div class="card-body">
                                        <div class="table-responsive">
                                            <table id="example" class="table table-striped table-bordered" >




    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ maisonhote.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ maisonhote.nom }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ maisonhote.adresse }}</td>
            </tr>
            <tr>
                <th>Num</th>
                <td>{{ maisonhote.num }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ maisonhote.prix }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ maisonhote.note }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ maisonhote.image }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-success" href="{{ path('maisonhote_index') }}">Retour liste</a>

    <a class="btn btn-success" href="{{ path('maisonhote_edit', {'id': maisonhote.id}) }}">Modifier</a>

    {{ include('maisonhote/_delete_form.html.twig') }}
{% endblock %}
