{% extends 'baseback.html.twig' %}

{% block title %}{% endblock %}

{% block body %}
    <h1></h1>

    <div class="app-content">
                    <div class="side-app">
                        <div class="page-header">
                            <h4 class="page-title"></h4>
                            <ol class="breadcrumb">
                                <li class="breadcrumb-item"><a href="#"><i class="fe   fe-box mr-1"></i> Tables</a></li>
                                <li class="breadcrumb-item active" aria-current="page">Data Tables</li>
                            </ol>
                        </div>
                        <div class="row">
                            <div class="col-md-12 col-lg-12">
                                <div class="card">
                                    <div class="card-header">
                                        <div class="card-title">Liste des réservations</div>
                                    </div>
                                    <div class="card-body">
                                        <div class="table-responsive">
                                            <table id="example" class="table table-striped table-bordered" >

   
        <thead>
            <tr>
                <th>ID</th>
                <th>Date d'entre</th>
                <th>Date de sortie</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for resmaison in resmaisons %}
            <tr>
                <td>{{ resmaison.id }}</td>
                <td>{{ resmaison.dateentre ? resmaison.dateentre|date('Y-m-d') : '' }}</td>
                <td>{{ resmaison.datesortie ? resmaison.datesortie|date('Y-m-d') : '' }}</td>
                <td>
                    <a class="btn btn-success" href="{{ path('resmaison_show', {'id': resmaison.id}) }}">Supprimer</a>
                    <a class="btn btn-secondary" href="{{ path('resmaison_edit', {'id': resmaison.id}) }}">Modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

     <div class="card-body">
   
    <a class="btn btn-primary" href="{{path('imprim')}}"> Imprimer </a>
    <a class="btn btn-primary" href="{{path('maisonhote_index')}}"> Consulter liste maisons </a>
    <a class="navigation">{{ knp_pagination_render(resmaisons) }}</a>

     </div>

     

   
{% endblock %}
